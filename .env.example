# ─────────────────────────────────────────────────────────────────────────────
# OSINT Data Pipeline - Environment Variables
# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
#
#   cp .env.example .env
#
# These variables are loaded by src/config_loader.py at runtime.

# ── SerpApi (used by src/scrape_serp.py) ─────────────────────────────────────
# SerpApi lets you pull Google search results as structured JSON.
# 1. Sign up at https://serpapi.com  (free tier: 100 searches/month)
# 2. Copy your API key from the dashboard
# 3. Paste it below
SERPAPI_KEY=

# ── Alternative: SerperApi ───────────────────────────────────────────────────
# SerperApi (https://serper.dev) is a similar service with different pricing.
# The included scrape_serp.py uses SerpApi by default, but you can adapt it
# to use SerperApi by changing the endpoint and request format.
SERPER_API_KEY=

# ── Other optional API keys ─────────────────────────────────────────────────
# These are placeholders for future collection scripts you might add.
NEWS_API_KEY=

# Override the default log level (DEBUG, INFO, WARNING, ERROR, CRITICAL).
# If not set, falls back to config/settings.yaml value.
LOG_LEVEL=
